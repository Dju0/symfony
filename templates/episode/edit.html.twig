{% extends 'base.html.twig' %}

{% block title %}Editer l'episode{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="my-4">Editer l'episode</h1>

        {{ include('episode/_form.html.twig', {'button_label': 'Update'}) }}

        <a class="btn btn-primary mt-3" href="{{ path('app_episode_index') }}">Revenir à la liste des épisodes</a>

        {{ include('episode/_delete_form.html.twig') }}
    </div>
{% endblock %}
